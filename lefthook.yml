pre-commit:
  commands:
    prettier:
      run: bin/format-staged prettier
      stage_fixed: true
    shfmt:
      run: bin/format-staged shfmt
      stage_fixed: true

pre-push:
  commands:
    tests:
      run: just tests-all
