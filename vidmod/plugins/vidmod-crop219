#!/usr/bin/env bash
# vidmod crop219

VERSION='3.0.0'

[ -n "${VIDMOD_LIB:-}" ] || VIDMOD_LIB="$(dirname "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)")/lib/vidmod.sh"

# shellcheck source=../lib/vidmod.sh
. "$VIDMOD_LIB"

crop219_start() {
	vidmod_legacy_start crop219 "$@"
}

vidmod_plugin_run "$@"
