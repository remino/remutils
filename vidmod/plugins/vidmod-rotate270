#!/usr/bin/env bash
# vidmod rotate270

VERSION='2.1.0'

[ -n "${VIDMOD_LIB:-}" ] || VIDMOD_LIB="$(dirname "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)")/lib/vidmod.sh"

# shellcheck source=../lib/vidmod.sh
. "$VIDMOD_LIB"

rotate270_start() {
	vidmod_legacy_start rotate270 "$@"
}

vidmod_plugin_run "$@"
