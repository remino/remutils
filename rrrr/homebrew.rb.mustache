# vim: set ft=ruby :
class {{formula_class}} < Formula
  desc "Create rsync snapshot backups over SSH"
  version "{{version}}"
  url "https://github.com/remino/remutils/releases/download/{{tag}}/{{tag}}.tar.gz"
  sha256 "{{sha256}}"
  license "ISC"
  homepage "https://github.com/remino/remutils/tree/main/{{name}}"
  {{#revision}}
  revision {{revision}}
  {{/revision}}

  depends_on "bash"
  depends_on "rsync"

  livecheck do
    url :stable
    strategy :github_latest
  end

  def install
    bin.install "{{name}}"
    man1.install "man/{{name}}.1"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/{{name}} -V")
  end
end
